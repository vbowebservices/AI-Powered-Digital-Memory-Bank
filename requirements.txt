# --- Flask stack (compatible pins) ---
Flask==2.2.5
Werkzeug==2.2.3
Flask-SQLAlchemy==2.5.1
SQLAlchemy==1.4.49

# --- Auth (fixes your current error) ---
Flask-JWT-Extended==4.5.3

# --- Production server for Render ---
gunicorn==20.1.0

# --- If you connect to Postgres on Render, keep this ---
psycopg2-binary==2.9.9

# --- Optional but commonly used ---
python-dotenv==1.0.1
